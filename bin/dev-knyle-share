#!/usr/bin/env ruby

require_relative "knyle-share-bootstrap"

KnyleShareBootstrap.setup!(__FILE__)
require "knyle_share"

config_store = KnyleShare::ConfigStore.new(
  env: ENV,
  default_config_name: "dev-config.json"
)

exit KnyleShare::Cli.new(config_store: config_store).run(ARGV)
