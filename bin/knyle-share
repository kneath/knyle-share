#!/usr/bin/env ruby

script_path = File.realpath(__FILE__)
repo_root = File.expand_path("..", File.dirname(script_path))

$LOAD_PATH.unshift(File.join(repo_root, "lib"))

require "knyle_share"

exit KnyleShare::Cli.new.run(ARGV)
